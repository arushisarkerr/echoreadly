/**
 * Supabase Storage identifiers used by EchoReadly.
 */

/** Private bucket for uploaded documents (files, HTML, YouTube payloads). */
export const PDFS_BUCKET = "pdfs";

/** Private bucket for generated TTS audio. */
export const AUDIO_BUCKET = "document-audio";

/** Private bucket for generated text exports. */
export const EXPORTS_BUCKET = "document-exports";
