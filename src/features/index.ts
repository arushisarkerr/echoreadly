/**
 * Feature modules.
 *
 * Organize by product capability, not by technical layer:
 *   features/auth/
 *   features/dashboard/
 *   features/upload/
 *   features/summary/
 *   features/reader/
 *   features/tts/
 *   features/settings/
 *
 * Each feature may contain its own components, hooks, types, and server helpers.
 * Routes in `app/` should stay thin and compose feature modules.
 */

export {};
