/**
 * Layout primitives shared across routes.
 * Keep navigation, headers, and page chrome here — not feature-specific UI.
 */

export { Container } from "./container";
export { RootShell } from "./root-shell";
