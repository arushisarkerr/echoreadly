/**
 * Shared presentational components reused by multiple features
 * (empty states, loading indicators, branding marks, etc.).
 *
 * If a component is only used by one feature, keep it inside that feature folder.
 */

export {};
