/**
 * Design-system UI primitives (buttons, inputs, typography atoms, etc.).
 *
 * These components must remain feature-agnostic and accessible.
 * Prefer Server Components by default; add `"use client"` only when interactivity requires it.
 *
 * Primitives will be added as product surfaces need them — none are stubbed here.
 */

export {};
