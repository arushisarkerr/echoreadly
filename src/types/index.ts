/**
 * Barrel export for shared TypeScript types.
 */

export type {
  Database,
  DocumentMetadata,
  Json,
  PaginatedResult,
  PaginationInput,
  PaginationMeta,
  ProcessingStatus,
} from "./backend";
export type { AsyncStatus, Result } from "./common";
