/**
 * Cross-cutting library helpers.
 *
 * Prefer deep imports to avoid pulling server code into the browser bundle:
 *   @/lib/supabase/client
 *   @/lib/supabase/server
 *   @/lib/supabase/middleware
 *
 * Capability folders:
 *   lib/supabase/    — database & auth client wrappers
 *   lib/auth/        — session and identity helpers
 *   lib/pdf/         — document parsing
 *   lib/ai/          — model provider adapters
 *   lib/storage/     — object storage
 *   lib/validators/  — shared schemas
 *   lib/security/    — rate limits, headers, validation, API errors
 *   lib/logger/      — structured server logging
 */

export {};
